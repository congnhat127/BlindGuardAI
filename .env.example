# Cấu hình môi trường (Không commit file .env thật lên git, chỉ copy file này ra thành .env và điền giá trị thật)

# Cloud Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=blindguard
DB_PASSWORD=your_secure_password
DB_NAME=blindguard_db

# Backend API
API_PORT=8000
JWT_SECRET=your_jwt_secret_key

# Camera / Edge AI Stream
CAMERA_RTSP_URL=rtsp://admin:admin@192.168.1.100:554/stream1
TENSORRT_MODEL_PATH=./1_AI_Processing_Edge/model_optimization/weights/yolo11s.engine
